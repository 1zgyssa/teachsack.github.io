# 2026-07-10 工作记录

## 项目梳理：TeachSack（教学工具箱）v1.2.4
- 教师用 Electron 桌面教学辅助软件，开发者"小张＆阿豪"，¥19.9 买断。
- 结构：main.js(主进程)/preload.js/教学工具箱.html(主界面,9024行单文件)/index.html(官网落地页)/server.js(授权API)/generator.html(激活码生成)/admin.html(后台)。
- 6 大模块：卡牌点名器、趣味挑战、随机分组、课堂计时器、知识闯关、成绩统计。

## 清理排查（仅分析，未删除）
根目录总占用 1.8G。明确可删的"垃圾"约 600MB+（不含 node_modules 244M 属构建依赖）：
- 7 个 dist* 构建产物（dist/dist2/dist-new/dist-new-2/dist_final/dist_new/dist_newest），共 ~423MB，其中 dist_newest 占 392MB。.gitignore 只忽略了 dist/ 和 dist_newest/，其余 5 个漏了。
- backup/67MB exe、downloads/68MB exe、gh.zip 13MB、gh-cli/ 49MB（GitHub CLI 工具，非源码）。
- 调试残留：find-asar.py、update-server.js、test_*.js/html/bat/ps1、test-*.js 系列、simple-*/test-icon.png、.trae/（Trae IDE 配置）、空的 .github/workflows。
- 一次性工具脚本：generate-icon.js/set-icon.js/generate-screenshot.js（产物 icon.ico 已存在）。

## 需用户确认的
- stable-toolbox.html（88K）：带移动端/PWA meta+manifest，疑似独立 web 版，待确认是否线上在用。
- server-deploy/（自带 .git）：Cloudflare Pages/Vercel 部署版授权后端，是独立仓库，混在源码里属冗余副本。
- latest.yml / service-worker.js / manifest.json / CNAME / vercel.json：官网相关，保留。

## .gitignore 缺陷
未忽略：dist2/、dist-new/、dist-new-2/、dist_final/、dist_new/、set-icon.js、generate-screenshot.js、test-update-debug.js、test-update-simple.ps1、simple-*.png、test-icon.png、admin.html、stable-toolbox.html、server-deploy/、backup/、downloads/、gh.zip、gh-cli/、.trae/。

## 已执行清理（方案一，2026-07-10 23:1x）
- 用户指定：dist_newest/ 保留（软件本体 exe）。
- 将以下"明确可删"项 MOVE 到 D:\jxgj\_cleanup_backup\（209MB，可恢复，未真正删除）：
  backup/(67M)、downloads/(68M)、gh-cli/(49M)、gh.zip(13M)、dist/dist2/dist-new/dist-new-2/dist_final/dist_new(共~14M)、
  .trae/、.github/(空)、find-asar.py、update-server.js、simple-*/test-icon.png、generate-icon.js/set-icon.js/generate-screenshot.js、
  全部 test_*/test-* 调试脚本。
- 关键保留项已逐一带确认：dist_newest、教学工具箱.html、main.js、preload.js、server.js、index.html、admin.html、generator.html、images/、sounds/、build/、icon.ico、stable-toolbox.html、server-deploy/、latest.yml、node_modules 全部✓。
- 已修补 .gitignore，补充上述漏忽略项。
- 待用户确认无误后，可删除 _cleanup_backup/ 真正释放 209MB。stable-toolbox.html 与 server-deploy/ 仍待用户确认用途。
