# 2026-07-13 工作日志


## 官网购买授权卡片比例优化（07:50）
- 用户反馈 index.html 购买授权卡片过大、空旷。
- 修改 index.html 926行起 .pricing-card 与 .buy-card 相关规则：卡片最大宽度从 900px→520px、padding 从 40px→28px 32px；图标/标题/价格/特性列表间距与字号均适度收紧；内部 buy-card 宽度 440px→360px、padding 与间距同步收紧。未删除任何文字/按钮/功能。
- CSS 大括号 218/218 平衡。git commit c62db99。
- 此改动仅官网展示页，无需重打包软件。

## 安装前端设计 Skill（08:23）
- 用户要求搜索安装 frontend-design skill。经 marketplace-skill-installer 搜索：推荐市场无 frontend-design 精确同名项（frontend/design 关键字均无匹配）。
- 提供相近项供选：impeccable v2.0.0(UI/UX设计工具集) / frontend-dev v0.1.1 / awesome-design-md v1.0.0。用户选 impeccable。
- 已安装 Impeccable（前端设计工具集）v2.0.0 到 ~/.workbuddy/skills/（skillId: skill_2053082862415904768）。

## 固化 TeachSack 设计上下文（08:33）
- 用户确认将 TeachSack 设计调性写入 impeccable 的 Design Context 文件。
- 新建 D:\jxgj\.impeccable.md，固化：产品概述/目标用户/核心用例/品牌调性、DO-DON'T 设计原则（去AI味·线条图标·双主题变量·浅色必查·性能隔离）、主题 Token、图标规范、弹窗规范、性能约束、协作约定。
- 重点防线：克制去AI味、线条SVG替代emoji、双主题CSS变量、严禁为视觉改动功能代码、保持自动更新通路。

## 官网整体 UI 去 AI 味重做（08:39）
- 用户要求用 impeccable 技能优化官网 index.html 整体 UI。
- 全量重写 <style> 与图标：删除紫蓝渐变背景+三色发光光斑(glow-effect)、毛玻璃滥用、霓虹阴影；改为冷静深底(#0b0f17)+单一品牌色(indigo)克制使用+中性阴影；引入 clamp() 字号层级。
- 约 40 处 emoji 图标（feature/product/showcase/download/platform/pricing/faq/hero/warning 及 JS 弹窗标题）全部替换为内联线条 SVG（stroke=currentColor, viewBox 0 0 24 24）。
- 守住底线：所有 section id、Gitee 下载链接×3、发卡网购买链接×1、15 个 JS 函数、全部中文文案均原样保留。
- 核验：残留 emoji=0、CSS 大括号 201/201 平衡、链接/函数/id 齐全。commit 8bd59b1，已推送 origin/main（GitHub Pages 仓库 teachsack.github.io）。

## 官网深色分层增强（08:55）
- 用户反馈重做后纯黑背景单调。诊断：区块同一 --bg、面板透明度过低(0.035)、品牌色过少。方向选"深色分层增强"（保持深色不做主题切换）。
- 纯 CSS 改动：--bg-elev 0x111726→0x131b2c、--panel 0.035→0.05、--panel-strong 0.06→0.085、新增 --shadow-card 与 --hairline-top（顶部细高光）。
- 区块明暗交替（hero/product-features/download/faq 用 --bg；features/showcase/pricing/footer 用 --bg-elev + 顶部高光台阶感）。
- 卡片加浮起阴影：feature-card/product-feature-card/download-card-item/faq-item + shadow-card 及 hover 加强；download-card/pricing-card 加大阴影。
- 背景光晕加第三处暖色(--gold)微光增纵深。核验 201/201 平衡、emoji=0、链接齐全。commit cdf44f3 已推送。

## 官网浅色暖调活力重做（09:10）
- 用户认为深色分层仍"死气沉沉"，教学软件官网应有生气。方向：整体翻浅色暖调（用户明确"先去掉深色"）。
- 配色系统翻转：--bg #0b0f17→#fcfbf7(暖白)、--bg-elev→#f5f1e8(米色分层)、--panel 白透、--text 深字；新增 --accent 珊瑚 #ff7a4d + --accent-soft。
- 背景光晕改柔和暖光(品牌靛蓝左上+珊瑚右上+金右下)；导航/弹窗改浅色磨砂；hero 数据数字用 --accent 活力焦点；导航下载按钮改珊瑚(与 hero 靛蓝主按钮双色对比)；按钮加柔和投影圆润感；section 小标签/截图投影适配浅色。
- 修正浅色下会"消失"的硬编码浅字(#a5b4fc→brand-strong、#e2e8f0/版本号→var)、弹窗内容改 --text-secondary。
- 核验 201/201、emoji=0、--accent 使用×2、链接齐全、JS 函数完好。commit 9e0be5b 已推送。

## 官网撞色油漆风重做（09:25）
- 用户要求用技能再做：背景两种颜色撞色（油漆/涂鸦感，不花哨），整站含顶部按键一个不漏。
- 按 impeccable 方法论：引入 ZCOOL KuaiLe/Baloo 2 圆润展示字体（标题）；根变量提炼 --indigo×--coral 双撞色系统（保留 brand/accent 别名兼容）。
- 背景：body::before 两个柔和模糊径向光斑（靛蓝左上+珊瑚右上+金右下）相撞；body::after 有机 blob 装饰层。
- 全界面统一双色：导航下载钮珊瑚/主按钮靛蓝/secondary 珊瑚描边；feature-card 顶部靛蓝·珊瑚·渐变三色条交替；product-feature 珊瑚；showcase 图标珊瑚；download 图标靛蓝/平台钮悬停珊瑚；pricing-card 珊瑚边框+右上油漆 blob/badge 渐变；buy-btn 珊瑚/payment-btn 靛蓝；FAQ 箭头靛蓝/social 靛蓝悬停；logo 图标 conic 双色圆。
- 修 JS 硬编码深色弹窗/toast：modal 改白底珊瑚边框、toast 改珊瑚、overlay 半透明模糊；payment-input 改深字（原 #fff 在浅底不可见）。
## 修复顶部导航栏残留旧深色样式（09:40）
- 用户截图反馈顶部导航栏仍是旧版深色，文字看不清。诊断：JS 滚动监听器在 window.scrollY>50 时把 nav 背景 inline 覆盖为 `rgba(11, 15, 23, 0.92)`，导致新浅色主题下顶部瞬间变回旧深色。
- 修复内容：
  1. 导航栏改用 CSS 类 `.scrolled` 控制，滚动后背景为白透 0.96（确保文字始终可读），并加靛蓝/珊瑚双色底部描边阴影，彻底告别旧深色。
  2. 导航链接色由 `var(--text-secondary)` 改为 `var(--text-primary)`，hover 改为珊瑚色，提升可读性。
  3. 替换旧版外部图片 logo（teachsack-icon.svg 为紫粉渐变+emoji）为内联 div：conic 双色不规则 blob 背景 + 白色线条毕业帽 SVG，保持油漆风统一。
## 导航栏 + 首屏极简重构（09:45，吸收 OpenAI 截图灵感）
- 用户要求从三张 OpenAI 官网截图中吸取「极简/留白/克制」设计思路，嫁接到 TeachSack 调性（非照搬）。先做第 1 项：导航栏 + 首屏。
- 导航栏：.nav-links 字重 600→500、字号 0.95→0.88rem、gap 2→1.8rem、颜色改 --text-secondary（hover 变 --text-primary）；下载钮从珊瑚实心改为深色胶囊（--text-primary 底 + 白字 + 999px 圆角 + hover 转靛蓝）；容器 padding 1→0.8rem 更紧凑。
- 首屏：padding 7/5rem→9/6rem 加大留白；副标题去底色/去边框/转大写灰字更含蓄；标题 clamp 上限 3.6→4.4rem、行高 1.18→1.12 更狠；描述精简为一句 + max-width 520；主按钮改珊瑚胶囊（唯一暖色强调），次按钮弱化为纯文字链接（无边框无底，hover 加间距）。
- 原则：珊瑚仅保留为首屏主按钮单一强调色，其余中性化，呼应 OpenAI 克制感但保留教学软件温度。
- 核验 209/209、emoji=0、链接齐全。commit e4b724f 已推送。


- 用户反馈新换的线条毕业帽 logo 不如原图标好，要求只改头像，其他不动。
- 将导航栏 logo 从内联 div+SVG 换回原来的 `<img src="images/teachsack-icon.svg">`（紫粉渐变+毕业帽 emoji），保留导航栏浅色与滚动态修复不变。
- 核验：teachsack-icon.svg 引用恢复、emoji 计数仅来自 SVG 文件引用本身（页面内联仍无 emoji）、CSS 大括号 209/209 平衡。commit 584e82b 已推送。

## 功能展示区大卡片化（09:50，吸收 OpenAI 截图灵感第 2 项）
- 用户要求做"减少小卡片，换成大图/大Feature"。改动三块纯 CSS + 局部 HTML，功能/链接零改动。
- 核心功能(features)：卡加大——icon 46→60px 填充柔色块、padding 2→2.6/2.2rem、加 min-height 218px、标题 1.2→1.32rem、描述行高 1.65→1.72、网格 gap 1.5→2rem、圆角 radius→radius-lg；顶部油漆撞色条保留。
- 产品特色(product-features)：3 张独立小卡合并为一条 cohesive 横幅 `.product-banner`（3 列 + 竖向分隔线），HTML 由 product-feature-card×3 重构为 product-point×3；移动端断点转 1 列 + 横向分隔线。
- 产品展示(showcase)：大图升级为中心——`.showcase-image::after` 加靛蓝×珊瑚柔光晕烘托"油漆"中心感；截图圆角 18→20px、阴影加深；侧边列表项放大（icon 40→50px、padding 1.3→1.5rem、标题 1.05→1.12rem、hover 边框改珊瑚）。
- 核验：CSS 大括号 212/212 平衡、emoji=0、gitee×3/kufaka×1 链接齐全、旧 product-feature-* 类零残留。commit b7277be 已推送 origin/main。

## 整体配色降温（09:58，吸收 OpenAI 截图灵感第 3 项）
- 用户要求"保留珊瑚作为唯一强调色，其余中性化"。纯配色改动，功能/链接零改动。
- 根变量重定义：--bg #fcfbf7→#fafafb（中性冷白）、--bg-elev #f5f1e8→#f1f2f6（冷灰分层）；--text-secondary #5b6172→#555c6b（更冷）。
- 靛蓝降级为板岩灰中性支持色：--indigo #5b5bf0→#5c6470、--indigo-strong→#474e59、--indigo-soft→rgba(92,100,112,0.10)；--brand 系列别名自动跟随。
- 珊瑚成为唯一彩色强调：--coral 保留 #ff7a4d，--accent 别名；金色彩 --gold 两处强调文字（buy-desc/activation-steps strong）改为珊瑚；JS 弹窗关闭钮 #5b5bf0→#ff7a4d。
- 背景油漆光斑降温：body::before 去掉靛蓝+金色光斑，改为"克制珊瑚暖光(0.14) × 中性冷光(0.10)"双调相撞（仍保留油漆涂鸦感但更冷静）；body::after blob opacity 0.5→0.38。
- 所有硬编码靛蓝/金色 rgba 转中性或珊瑚（导航阴影、logo 阴影、小标签描边、feature hover 边框、showcase 光晕、下载/支付按钮阴影、pricing 背景与卡片阴影等）。
- 校验：CSS 212/212、emoji=0、gitee×3/kufaka×1 齐全、硬编码靛蓝 rgba(91,91,240) 与金色 rgba(224,150,47) 与 #5b5bf0 均清零、var(--gold) 清零、珊瑚强调×18、板岩灰中性×15、JS 函数完好。commit aeaf41b 已推送。

## 统一功能卡顶部颜色（10:07）
- 用户截图反馈核心功能卡片顶部颜色条混乱（靛蓝/珊瑚/渐变交替）。
- 修改 index.html 428 行起：将 `.feature-card::before` 顶部条统一为 `var(--coral)`，删除第 2、3 张卡的 `:nth-child` 覆盖，让 6 张功能卡顶部颜色一致。
- 纯 CSS 改动，功能/链接/文案/JS 均不变。
- 校验：CSS 大括号 266/266 平衡、true emoji=0、gitee 下载链接×3、kufaka 购买链接×1、section id 7 个齐全。commit aaabf58 已推送 origin/main。

## 功能卡顶部条改深色（10:12）
- 用户反馈珊瑚色统一后与冷白主题不协调、太鲜艳。改为统一深色 `var(--text-primary)`（#1f2330 深蓝灰黑），形成干净的明暗对比，更克制。
- 纯 CSS 改动，功能/链接/文案/JS 零改动。
- 校验：大括号 266/266、emoji=0、链接/section 齐全。commit c8cac7c 已推送 origin/main。

## 集成原生 WebGL 流动渐变背景（10:21，融入 Shader Gradient 风格）
- 用户希望官网背景更吸引人，不像冷白单调。调研确认 shadergradient 官方是 React+Three.js 重型组件；纯静态单文件 HTML 采用**原生 WebGL 片段着色器**方案（零外部依赖、性能稳、调性可控）。
- 实现：
  1. CSS 新增 `#shader-bg`：fixed 全屏、z-index:-2、pointer-events:none、blur(55px) saturate(120%) 柔化、opacity 0.85；位于 body::before/::after 静态光斑之下。
  2. HTML `<body>` 后插入 `<canvas id="shader-bg">`。
  3. JS 追加 `initShaderBackground()` IIFE：fbm 域扭曲流动渐变（珊瑚暖晕+板岩灰冷晕+冷白基，低饱和慢速），全屏三角形、requestAnimationFrame、resize 降分辨率(0.45x)、visibilitychange 暂停省电；WebGL 不可用时隐藏 canvas 回退静态光斑。
- 守住底线：功能/链接/文案/section 均零改动；仅新增背景层，不触碰现有 JS 逻辑。
- 校验：大括号 284/284、emoji=0、gitee×3/kufaka×1、section 7、canvas+GLSL 存在。commit 45cff2f 已推送 origin/main。

## 接入官方 Shader Gradient 组件（真 3D 原生质感）（10:30）
- 用户认为 2D fbm 背景"差点意思"，想要 shadergradient 原生 3D 质感（光照/颗粒/立体流动）。确认路线：引入真·官方组件（用户选"引入真组件"）。
- 调研：shadergradient 现包 `@shadergradient/react@2.4.20` 暴露 `ShaderGradientCanvas`+`ShaderGradient`，支持 `control="query"` + `urlString` 直接加载 shadergradient.co/customize 配置；依赖 React19 + R3F v9 + three。验证 esm.sh 各 URL 均 200。
- 集成（纯静态单文件 HTML 方案）：
  1. 用 `<script type="importmap">` 映射 react@19.2.1 / react-dom@19.2.1 / three@0.171.0 / @react-three/fiber@9.2.0(?external) / @shadergradient/react@2.4.20(?external) / htm@3.1.1。
  2. `<script type="module">` 用 htm 无 Babel 渲染 `<ShaderGradientCanvas><ShaderGradient control="query" urlString="...customize?...color1=%23ff7a4d&color2=%23fafafb&color3=%235c6470&lightType=3d&type=plane&grain=on&uSpeed=0.4..."/></ShaderGradientCanvas>`，配色珊瑚+冷白+板岩灰。
  3. 保留原 2D `#shader-bg` canvas 作 fallback：仅当 React 真正渲染出 `<canvas>`（setTimeout 1.8s 检测）才给 body 加 `shader-react-loaded` 类隐藏 2D；CDN 失败/模块报错则 2D 自动兜底。
- 守住底线：功能/链接/文案/section 与 2D 背景 init 逻辑全保留，仅新增 3D 层与 importmap。
- 校验：大括号 301/301、emoji=0、gitee×3/kufaka×1、section 7、importmap/module/ShaderGradientCanvas/urlString/fallback 标记齐全。commit 31cb3dd 已推送 origin/main。
- 注意：3D 组件依赖 esm.sh CDN（约 600KB+），本环境无法跑真实浏览器验证运行时组合；已加 2D fallback 兜底。用户刷新页面后需确认实际效果，若 CDN 不稳可后续改视频背景方案。

## 背景切换为球体模式 sphere（10:32）
- 用户"试试别的背景效果"，给 4 选项（sphere/water/更灵动plane/极简留白），选 sphere 球体立体光感。
- 仅改 shadergradient urlString：`type=plane`→`type=sphere`，参数 cAzimuthAngle=180&cDistance=3.9&cPolarAngle=115&uDensity=1.3&uSpeed=0.35&uStrength=0.4&reflection=0.1&positionX=-0.6&positionY=0.1&rotationZ=235&fov=45，配色珊瑚+冷白+板岩灰不变。
- 用户截图反馈还是平面效果。排查发现 URL 中混入了 sphere 不支持的 `cPolarAngle` 参数（类型定义：cPolarAngle 只适用于 plane/waterPlane；cDistance 才是 sphere 专用）。移除 cPolarAngle，保留 cAzimuthAngle + cDistance 作为 sphere 视角参数，避免组件解析时 fallback 到 plane。
- 校验：大括号 301/301、emoji=0、gitee×3/kufaka×1、section 7、type=sphere 且 URL 无 cPolarAngle 残留。commit 1f2c412 已推送 origin/main。刷新时建议清缓存（Ctrl+Shift+R）以确认新参数生效。

## 移除 3D 组件、2D 着色器升级为 sphere 球体光晕（10:40）
- **根因确认**：用户多次刷新仍看到平面雾感（非球体），且 localhost 沙箱服务器用户本地浏览器访问不到。判定 3D 组件从未成功加载——`@shadergradient/react` 依赖 esm.sh 境外 CDN，中国大陆网络拦截致静默失败，2D fallback 顶上。用户最终选方案二（零依赖 2D 升级版）。
- 改动：
  1. 移除全部 3D 依赖：`<script type="importmap">`、`<script type="module">`（ShaderGradientCanvas/ShaderGradient/htm）、HTML 中 `<div id="shader-bg-react">`、CSS 中 `#shader-bg-react` 与 `body.shader-react-loaded #shader-bg{display:none}` 规则、`<body>` 处重复 canvas（修掉之前误改导致的双重 canvas/div）。
  2. 2D 着色器从平面 fbm 重写为「带光照的发光球体」：`ballShade()` 生成球面明暗（NdotL 漫反射+软边），珊瑚主球偏左下缓慢呼吸移动、板岩灰辅球偏右上冷晕，冷白基底；CSS `#shader-bg` blur 从 55px 降到 30px 保留球体形状。
- 守住底线：功能/链接/文案/现有 JS 逻辑零改动，仅改背景层；WebGL 不可用时仍回退静态光斑。
- 校验：大括号 285/285、emoji=0、gitee×3/kufaka×1、section 7、无 esm.sh/`@shadergradient`/importmap/module 残留、2D canvas+`ballShade` 存在。commit 97c6809 已推送 origin/main。
- **关键教训**：单文件官网不要依赖境外 CDN（esm.sh 等）做核心视觉；需"原生 3D 质感"应优先零依赖 WebGL 自绘或视频背景方案。

## 球体背景动态与立体感增强（10:57）
- 用户反馈当前球体背景偏静态、立体感不足（像色块）。
- 优化点：
  1. CSS `#shader-bg` 模糊从 28px 降至 20px，透明度从 0.92 提至 0.95，保留球体轮廓。
  2. fragment shader 重写：
     - `u_time` 速度从 0.25 提到 0.45，整体动画变快。
     - 主球/辅球中心移动幅度从 0.03-0.05 增大到 0.08-0.14，肉眼可见移动。
     - 添加半径呼吸：`R1 = 0.50 + 0.05 * sin(t*0.7)`、`R2 = 0.32 + 0.03 * cos(t*0.6)`。
     - 增强 3D 立体感：加入 **Fresnel 边缘光**（rim = pow(1.0 - NdotV, 3.0) * 0.45）、更强高光（spec * 0.32）、高光随时间缓慢游走（pulse）。
     - 新增小「卫星球」围绕主球公转，增加灵动感和空间层次。
     - 背景加入微弱冷灰呼吸，让整体不再是静止的。
- 守住底线：功能/链接/文案/section/JS 逻辑零改动，仅改背景层着色器。
- 校验：大括号 285/285、圆括号 560/560、emoji=0、gitee×3/kufaka×1、section 7、无 3D 组件残留、`ballShade`/`c3 = c1`/`rim = pow`/`blur(20px)` 均存在。commit 20186a2 已推送 origin/main。

## 背景更清晰 + 梦幻化（11:11）
- 用户反馈：球体已正常运动，但背景偏模糊、希望更明显，并优化得更梦幻。
- 改动：
  1. CSS `#shader-bg` 模糊 20px → 13px，saturate 120%→125%，让球体轮廓与光晕更清晰可见。
  2. fragment shader 升级（梦幻化）：
     - `ballShade` 函数签名加 `out vec3 glow` 参数，新增**外层柔光晕**（smoothstep(1.7,0.9,dist)*0.45*baseCol），球体像发光体。
     - 背景从单调冷灰呼吸改为**流动极光**：两层 fbm 分别向 coral/slate 平滑插值，缓慢漂移，呈柔彩云絮感。
     - 新增 `dust()` 函数：3 层漂浮光尘网格粒子，随 hash 抖动并 twinkle 闪烁，营造梦幻星尘。
     - 主球/辅球/卫星球本体前先叠加各自 glow，再叠加光尘，最后 `mix(white, col, 0.90)` 保留浅调。
- 守住底线：功能/链接/文案/section/JS 逻辑零改动，仅改背景层着色器与 CSS 模糊。
- 校验：大括号 287/287、圆括号 588/588、emoji=0、gitee×3/kufaka×1、section 7、无 3D 组件残留、`ballShade`/`float dust`/`out vec3 glow`/`blur(13px)` 均存在。commit 7cb874e 已推送 origin/main。
- 注：WebGL1 GLSL ES 1.00 支持函数 `out` 参数，已确认可用。

## 背景换为极光光幕（11:18）
- 用户认为球体光晕耐看但"容易腻、不够眼前一亮"。给 4 个零依赖方向（极光光幕/流光粒子流/流动等高线/液态虹彩），用户选**极光光幕**。
- 改动：彻底移除 `ballShade()`（含 `out vec3 glow`）与三层球体逻辑，改用 `auroraCurtain()` 极光帘幕函数：
  - 用 fbm 让帘幕中心线在垂直方向起伏（center = 0.40 + warp*0.26 + 0.12*sin(...)）。
  - 垂直光带 soft mask（smoothstep(0.34,0,d)）叠加高频率 fbm 光丝（rays），形成北极光般的流动帘幕。
  - 颜色沿 x 在 colA/colB 间流动。
  - 三层帘幕：珊瑚↔淡紫(0.95)、淡紫↔板岩灰(0.70)、珊瑚↔白(0.55)，叠加出丰富流动的彩光。
  - 保留 `dust()` 光尘（系数 0.4）维持梦幻星尘感。
  - 速度 `u_time * 0.5`。CSS 模糊维持 13px。
- 守住底线：功能/链接/文案/section/JS 逻辑零改动，仅改背景层着色器。
- 校验：大括号 287/287、圆括号 552/552、emoji=0、gitee×3/kufaka×1、section 7、无 `ballShade`/`out vec3 glow`、有 `auroraCurtain`/`float dust`、无 3D 组件残留。commit 4bd8462 已推送 origin/main。

## 修复极光背景纯白输出（11:25）
- 用户反馈：刷新后看到纯白色背景，极光似乎未显示。
- 根因：原极光着色器在接近白色的 `white=(0.972,0.976,0.990)` 底上用 `col += auroraCurtain(...) * 0.95` 做 additive 混合，高亮颜色加在白底上极易过曝到 (1,1,1)，导致整体发白。
- 改动：
  1. 改用 `mix(col, white + auroraCurtain(...), 0.72/0.55/0.42)` 逐层 blending，避免 additive 过曝。
  2. 基础底色改为 `vec3(0.92,0.93,0.97)`（浅冷灰），并加顶部渐变，让极光有空间显现。
  3. 加浓珊瑚 (`1.0,0.42,0.25`) 和淡紫 (`0.60,0.52,1.0`)，新增天蓝色 (`0.55,0.70,0.95`) 作为流动高光。
  4. 极光帘幕更窄更亮（`smoothstep(0.28,0,d)`），中心线起伏幅度更大 (`warp*0.32 + 0.18*sin(...)`)。
  5. 新增柔和暗角 `vignette`，中间更突出、四角略深，提升画面张力。
  6. 最终加 `pow(col, 0.95)` 压一点高光，让颜色更实。
- 守住底线：功能/链接/文案/section/JS 逻辑零改动，仅改背景层着色器。
- 校验：大括号 287/287、圆括号 563/563、emoji=0、gitee×3/kufaka×1、section 7、无 `ballShade`/`out vec3 glow`、无 `col += auroraCurtain`、有 `vignette`/`vec3 sky`、无 3D 组件残留。commit 3185cea 已推送 origin/main。
- 注意：因 GitHub Pages 缓存，用户刷新时仍建议 Ctrl+Shift+R 硬刷新；如仍有纯白，需检查浏览器 F12 Console 是否有红色 WebGL 报错。

## 进一步修复背景纯白 + 加 CSS fallback（11:31）
- 用户反馈：仍看到纯白背景，截图显示是通过 `file://` 协议直接打开本地文件，且 Console 出现 "'file:' URLs are treated as unique security origins" 警告。
- 根因分析：
  1. 极光着色器虽然改用 mix，但仍在 `mix(col, white + auroraCurtain(...), ...)` 里加 white，混合结果仍偏白；
  2. 用户直接双击文件以 `file://` 打开，浏览器可能因此对 WebGL/Canvas 施加更严格限制，且 GitHub Pages 版本和本地文件版本不一致。
- 改动：
  1. 着色器基础底色改为 `vec3(0.86,0.88,0.93)`（更明显的浅冷灰），不再加 white。
  2. 三层极光全部用 `col = mix(col, aN, clamp(...).x * 0.85/0.70/0.55)` 直接以极光颜色 blending，彻底移除 `white +` 提亮。
  3. 加浓珊瑚 (`1.0,0.40,0.22`)、浓淡紫 (`0.62,0.50,1.0`)、亮天蓝 (`0.40,0.65,1.0`)；极光带更窄更亮，中心线起伏幅度更大。
  4. `body::before` 改为更可见的径向渐变 fallback（珊瑚 0.28 / 板岩灰 0.22 / 底部珊瑚 0.16），确保即使 WebGL 失败或 Canvas 被隐藏，背景也绝不是纯白。
  5. 光尘系数提到 0.14/0.6，暗角更柔，整体色彩更实。
- 守住底线：功能/链接/文案/section/JS 逻辑零改动，仅改背景层着色器与 body::before fallback。
- 校验：大括号 287/287、圆括号 570/570、emoji=0、gitee×3/kufaka×1、section 7、无 `white + auroraCurtain`、有 `vec3(0.86, 0.88, 0.93)`、有 `clamp(a1, vec3(0.0), vec3(1.0)).x * 0.85`、有 CSS fallback 珊瑚/板岩灰、无 3D 组件残留。commit d8f420f 已推送 origin/main。
- 建议：不要直接双击文件用 `file://` 打开，请访问 https://teachsack.github.io/ 或本地 http 服务器；直接打开 file 协议可能导致 WebGL 安全限制和缓存问题。

## 方案B：本地 HTTP 预览服务器（11:48）
- 用户问"单文件 HTML 怎么更兼容原生动态背景"，澄清根因：单文件格式本身不是问题，现有背景已是零依赖原生 WebGL 着色器；真正短板是 (1) file:// 双击打开受 WebGL 安全限制、(2) 早期 3D 版依赖 esm.sh 境外 CDN 被墙。
- 给出三路径对比：B 本地 HTTP 服务（零改动）/ C 多文件拆分 / D Vite+three 构建。用户选 B。
- 执行：新增 `serve.py`（仅用标准库 http.server，绑定脚本目录、关闭缓存、默认端口 8000），实测 `http://localhost:8000/` 返回 200、98845 字节、含 shader-bg×3 / initShaderBackground×1 / canvas#shader-bg×1，确认 http 下原生背景随页面正确提供（WebGL 在浏览器内渲染，这正是 file:// 缺失的一环）。
- 发现 serve.py 在更早会话已写好并提交（commit fb1e6f2 "chore: add zero-dep local preview server (plan B)"）但未推送；本次补推 origin/main（d8f420f..fb1e6f2）。功能/链接/样式零改动。
- 结论：线上 teachsack.github.io 本来正常，本地只需 `python serve.py` 用 http 打开即可看到原生动态背景，彻底规避 file:// 白屏。

## 路线A：Vite 打包真·Shader Gradient 动态背景（12:05–12:30）
- 用户选路线A：用 Vite 把官方 `@shadergradient/react` + three.js 打包进单文件 HTML，彻底摆脱 esm.sh，国内离线可用、与 shadergradient.co 官网效果一致。
- 工程结构：`app/`（Vite 源码）+ 仓库根 `index.html`（构建产物，GitHub Pages 部署）。`app/` 含 package.json / vite.config.js / index.html(源) / src/main.jsx。
- 关键技术点：
  1. `@shadergradient/react@2.4.20` 的 dist 是预打包 chunk，外部裸依赖仅 5 个：react、react-dom、three、@react-three/fiber、framer（组件内 socket.io 的 XMLHttpRequest 仅 dev 工具，正常渲染不触发）。
  2. 该组件按 R3F8+React18+three0.169 构建 → 锁定 react@18.3.1 / R3F@8.18 / three@0.169 / framer@2.4.1 / react-dom@18.3.1；npm install 需 `--legacy-peer-deps`（R3F8 要求 react<19，但旧 node_modules 残留触发 ERESOLVE，版本其实兼容）。
  3. **control="props" + 直传 color1/2/3/type 等**：组件代码无任何 fetch()，故零运行时联网（"query" 模式才会拉 shadergradient.co/customize，已避开）。
  4. JSX 必须放 `.jsx`（Vite 默认不对 `.js` 做 JSX 转译，否则构建报 invalid JS syntax）。
  5. vite.config：react() + vite-plugin-singlefile()，`base:'./'`，`cssCodeSplit:false`/`assetsInlineLimit` 拉满 → 产出完全自包含单文件。
- 为"零外部依赖"移除 `<head>` 的 Google Fonts `<link>`（Baloo 2 / ZCOOL KuaiLe）→ 回退系统字体栈（后续如需圆体可改为自托管 woff2）。
- 校验（dist/index.html）：esm.sh=0、googleapis=0、gstatic=0、外链 `<script>/<link>`=0、url(https=0；WebGLRenderer×5 证明 three 已内联；shader-bg 为 div；内容完整 gitee×3/kufaka×1/section id×7。
- 部署：`npm run deploy`（vite build + cp dist/index.html ../index.html）已加进 package.json；本次手动 cp 后 git add index.html app/* 提交 ad66b60 并推送 origin/main（fb1e6f2..ad66b60）。功能/链接/文案零改动。
- 教训固化：单文件官网做"原生 3D 质感"走本地打包（Vite singlefile）而非 CDN；配色/类型在 app/src/main.jsx 的 ShaderGradient props 里调。

## 背景切换为 waterPlane 灵动版（12:26）
- 用户对 sphere 版本满意，但想试"更灵动"形态，选 waterPlane（水面波光）。
- 改动 app/src/main.jsx 的 ShaderGradient props：type sphere→waterPlane；uSpeed 0.4→0.55、uDensity 1.2→1.4、uStrength 3.0→2.8、cDistance 3.4→4.2、cPolarAngle 95→90、positionX/Y -0.4/0.1→0/0、rotationZ 20→0、fov 42→50、reflection 0.1→0.3；grain/animate/lightType(3d)/配色(珊瑚+板岩灰+冷白)不变。
- 构建：npm run deploy（vite build + cp dist/index.html ../index.html）。校验：waterPlane×6、esm.sh=googleapis=gstatic=外链 script/link=0、WebGLRenderer×5 内联、gitee×3/kufaka×1 完整。
- 提交 0f80f77 并推送 origin/main（ad66b60..0f80f77）。功能/链接/文案零改动。

## waterPlane 相机角度优化（12:30）
- 用户反馈切换到 waterPlane 后与 sphere 看起来没区别。截图显示仍是球状/团块状。
- 根因：第一版 waterPlane 参数沿用 sphere 的 cPolarAngle=90（接近正侧/俯视）+ positionY=0，让水面平面看起来还是团块。从组件内置 preset 看，waterPlane 典型用 cPolarAngle 70-80、positionY 0.7-0.9 才能呈现"俯视看水面"的流动感。
- 改动：参考组件内置 `mint` waterPlane preset，改为 cPolarAngle 70、cDistance 4.4、positionY 0.7、uSpeed 0.3、uDensity 1.2、uStrength 3.4、uAmplitude 0、fov 45、reflection 0.1。
- 构建：npm run deploy。校验：waterPlane×6、esm.sh=googleapis=gstatic=外链 script/link=0、WebGLRenderer×5 内联。
- 提交 6e3d0b8 并推送 origin/main（0f80f77..6e3d0b8）。功能/链接/文案零改动。
- 提示：GitHub Pages 与浏览器缓存可能导致新效果延迟，需 Ctrl+Shift+R 硬刷新。

## waterPlane 配色改为水感海洋层次（12:35）
- 用户满意水波形态，但要求配色"更像水"。将原本 TeachSack 调性（珊瑚 #ff7a4d / 板岩灰 #5c6470 / 冷白 #fafafb）改为海洋层次：color1 #48cae4（浅水蓝）、color2 #0077b6（深湖蓝）、color3 #caf0f8（浅青白）。
- 改动仅 app/src/main.jsx 的 color1/2/3 + 注释；npm run deploy 重建。
- 提交 7682f3a 并推送 origin/main（6e3d0b8..7682f3a）。功能/链接/文案零改动。
- 备注：若以后想兼顾品牌珊瑚点缀，可在 color1/2/3 中保留一个珊瑚色（如 color1 改回 #ff7a4d 作为高光），其余两色用蓝系。

## waterPlane 清新透明水感版（12:41）
- 用户反馈截图：背景磨砂颗粒感重、整体颜色太深、深色文字看不清。
- 优化方向：去掉颗粒 + 大幅提亮 + 柔化波纹密度，保留水波动感。
- 改动 app/src/main.jsx：color1 #e0f7fa（浅青白）、color2 #26c6da（清澈青蓝）、color3 #ffffff（纯白）；grain on→off；uSpeed 0.3→0.25、uDensity 1.2→0.8、uStrength 3.4→2.0、reflection 0.1→0.05。
- 构建：npm run deploy。校验：waterPlane×6、grain×1、新色值各×1、esm.sh=googleapis=gstatic=外链 script/link=0、WebGLRenderer×5 内联。
- 提交 488d13a 并推送 origin/main（7682f3a..488d13a）。功能/链接/文案零改动。
- 后续兜底：如文字仍不够清晰，可在 hero 文字区加一层半透明白色渐变遮罩（CSS，不依赖组件）。

## 水波贯穿全站 + 配色加深恢复飘荡（12:48）
- 用户三点反馈：①整体淡过头 ②缺水波飘荡感 ③部分页面实心白底、部分水波（不一致）。
- 根因定位：整块 section 中 `.features`(bg-elev)、`.product-features`(bg)、`.showcase`(bg-elev)、`.footer`(bg-elev) 用实色背景盖住 z-index:-2 的 #shader-bg，而 hero/download/pricing/faq 透明故透出水波 → 对半分布。另 `body::before/::after` 两个珊瑚光晕 fallback 层(z-index:-1) 盖在水波上方、泛红且不统一。
- 改动：
  - app/src/main.jsx：配色加深到居中版 color1 #80d4e6 / color2 #0092b3 / color3 #effbff（比最初浅、比"清新版"深）；恢复水波飘荡 uSpeed 0.25→0.4、uDensity 0.8→1.2、uStrength 2.0→3.0、reflection 0.05→0.08；grain 保持 off。
  - app/index.html CSS：移除 `body::before`/`body::after` 两个 fallback 光晕层；`.features`/`.product-features`/`.showcase`/`.footer` 实色背景改 `transparent`，全站水波贯穿。卡片 --panel/--panel-strong 半透明保留（可读性不变）。body 的 --bg 仅作 shader 失效兜底。
- 构建：npm run deploy。校验：body::before=0（已移除）、transparent 段=6、waterPlane×6、esm.sh=googleapis=0、WebGLRenderer×5 内联。
- 提交 2d274a2（根index.html+main.jsx）并补 626ae5f（app/index.html 源）推送 origin/main（488d13a..626ae5f）。功能/链接/文案零改动。

